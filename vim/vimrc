" Enable file type detection
filetype on
filetype plugin on
filetype indent on

" Set scrolling options
" - Timeouts are in milliseconds, and are shortened to improve responsiveness
set mouse=a
set scroll=1
set timeoutlen=600
set ttimeoutlen=10
set ttyfast
set lazyredraw

" Increase max pattern length, prevents syntax highlighting errors w/long
" strings
set maxmempattern=5000

" Line break settings
set textwidth=0
set wrapmargin=0

" Enable line numbers
set number relativenumber

" Set leader key to space
let mapleader = " "

" Shortcuts for copy to system clipboard (default leader key = '\')
nnoremap <leader>y "+y
vnoremap <leader>y "+y

" Enable full color support
set termguicolors

" Plugin Start - vim-plug
call plug#begin()

" Plugins
Plug 'sainnhe/gruvbox-material'
Plug 'catppuccin/vim', { 'as': 'catppuccin' }

" Plugin End - vim-plug
call plug#end()

" Enable syntax highlighting
syntax on

" Fallback to built-in
colorscheme retrobox

" Fail silently if plugin not installed
silent! colorscheme catppuccin_macchiato

" Automatically set light/dark mode based on system setting
" let theme = system('defaults read -g AppleInterfaceStyle')
" if theme =~ 'Dark'
"     set background=dark
" else
"     set background=light
" endif

